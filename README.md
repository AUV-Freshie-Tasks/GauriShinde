# GauriShinde
For AUV software assignment

Dec 8 2025
Repo contains:

MatrixLibrary
Matrix.h: Header file, contains declaration of all functions.
Matrix.cpp: contains implementations of all functions.
Application.cpp: contains examples and test cases.


Dec 13 2025
Repo contains:

MatrixLibrary
Matrix.h: Header file, contains declaration of all functions.
Matrix.cpp: contains implementations of all functions.
Application.cpp: contains examples and test cases.

MatrixPy
Matrix.h: Header file, contains declaration of all functions. (declaration different (const added) than the one in MatrixLibrary)
Matrix.cpp: contains implementations of all functions. (declaration different (const added) than the one in MatrixLibrary)
MatrixPybind.cpp: exposes the functions to python.
CMakeLists.txt: make file.
Build
test.py: contains examples and test cases.
